# Name of the vault to reload the plugin in after each build.
# Run `obsidian vaults` to list the vaults Obsidian knows about.
OBSIDIAN_VAULT=

# Plugin to reload. Defaults to the "id" field in manifest.json.
# Only set this if your folder under .obsidian/plugins is named something else.
# OBSIDIAN_PLUGIN_ID=
